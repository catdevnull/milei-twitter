docial.nulo.lol {
	# Cloudflare terminates public TLS. Keep the origin encrypted without
	# depending on public ACME challenges through the proxied DNS record.
	tls internal
	encode zstd gzip
	reverse_proxy 127.0.0.1:3000
}
